#!/bin/env ruby

gui do
  install "brave"
  install "visual-studio-code"
  install "zoom"
  install "vlc"
  install "steam"
  install "discord"
  install "spotify"
  install "slack"
  install "gimp"
  install "qemu"
  install "localsend"
  install "xournalpp"
  install "alacritty"

  macos do
    install "flux"
  end

  windows do
    install "flux"
  end
end

unix do
  install "zsh"
  install "zellij"
  install "rbenv"
  install "nvm"
  install "rcm"
  install "antigen"
  install "vundle"
  install "heroku"
  install "ripgrep"
  install "btop"
  install "fzf"
  install "fd"
end

install "ollama"
install "fastfetch"
install "vim"
