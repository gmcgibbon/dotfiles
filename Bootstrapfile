#!/bin/env ruby

gui do
  install "firefox"
  install "visual-studio-code"
  install "skype"
  install "vlc"
  install "steam"
  install "spotify"
  install "slack"

  macos do
    install "iterm2"
    install "flux"
  end

  windows do
    install "discord"
    install "flux"
  end
end

unix do
  install "zsh"
  install "rbenv"
  install "nvm"
  install "rcm"
  install "antigen"
  install "vundle"
  install "heroku"
end

install "vim"
